spring:
  application:
    name: artdiary
  profiles:
    default: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

jwt:
  secret: ${JWT_SECRET_KEY}
  access-token-expired-time: ${JWT_ACCESS_TOKEN_EXPIRED_AT}
  refresh-token-expired-time: ${JWT_REFRESH_TOKEN_EXPIRED_AT}
